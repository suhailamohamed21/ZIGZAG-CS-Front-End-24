function rearrangeArray(array) {
    // Create a new array with elements in reverse order
    return [array[2], array[1], array[0]];
}

const array = ['tail', 'body', 'head'];
console.log(rearrangeArray(array)); 
